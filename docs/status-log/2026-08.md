# 状态历史日志 · 2026-08

> 本文件是只追加的历史层：只能从文件尾新增已核验回执，不得改写、删除或重排既有条目。`docs/status.md` 只保存当前层。

## 2026-08-02 状态拆层迁移基线

- 迁移动作：将拆层前主工作区 `docs/status.md` 的完整工作副本原文封存于下方；当前事实另在当前层重建。
- 来源完整性：原文件 14,529 bytes、158 行；Git blob `b9ba65e8c6cc8e71011429971132658ee857a196`；SHA-256 `6deb0fed42a65b86cca0b2c75b289ae8fe42d0e6190b01de1f2ae41b3e5b9e1c`。
- 解释边界：这是迁移前历史快照，不是当前事实；其中当时的阻碍、旧 head、旧队列和环境描述均按原文保留，后续核验只能在文件尾追加。

### 拆层前 `docs/status.md` 原文

```markdown
# 当前开发状态

> 本文件保存项目结果队列和固定候选；Conversation / Turn 运行状态在 Git 外控制系统。
> 项目 Agent 仅在确有隔离、候选或恢复治理需要时建立 Task / Run，并把恢复事实留在项目
> checkpoint；裁决入口和新现场会话不默认登记。

| 字段 | 内容 |
| --- | --- |
| `state` | `ready` |
| 更新时间 | `2026-08-02` |
| 当前产品目标 | PR #54 已合并且确认不触发运行产品部署；检索 badcase 候选已进入 Draft PR #55，当前只处理候选与 main 的状态冲突，不扩大功能域 |
| 当前集成任务 | `retrieval-badcase-loop`；Draft PR [#55](https://github.com/suuny-ab/traceable-support-agent/pull/55)，head `45db89f` |
| 复杂度 | 标准 / `R2`；已发生公开 GitHub 分支、PR 和 `main` 写入；没有 Provider 调用，PR #54 的自动部署链按 `governance_only` 跳过 deploy |
| 风险 / 成熟度 | 候选直接针对同一 16 题公开开发集调优，只能证明该开发集的来源覆盖变化；不是未见集、回答质量、线上成功率或发布结论 |
| 产品候选 | PR #54 已 squash merge 为 `8a306165221387805ee33e6c20b45d9260c48658`；检索 Draft PR #55 精确 head `45db89fee48c6534e4559d32b3134ba5704ce855` 已公开，未合并 |
| 项目基线 | `origin/main` 当前 head（本文件不固定基线 SHA，避免合并后失真）；唯一权威位置为主 worktree `traceable-support-agent` |
| 活动工作 | 无 |
| 最近完成 | PR #54 已合并；main CI `30738245420` 成功，部署链 `30738264198` preflight 成功且 deploy 按合同跳过；公网健康保持 `release_sha=915ca4e…`。检索分支已推送并创建 Draft PR #55 |
| 阻碍 | PR #55 当前 `mergeable=false`；只读 merge-tree 确认冲突仅在 `docs/status.md`，且尚无 PR workflow run。同步 main 会产生新 head，未包含在本次精确候选授权内 |
| Provider | 生产已启用（`2026-07-29`，用户显式授权）：`provider_enabled=true`；当前公网健康返回 `release_sha=915ca4ef7820870ee42fbef69ea719498d7f402d` 与 `live_experience=available`。本增量 Provider 调用 `0` 次，没有创建产品运行，也未改变凭据、预算或默认检索后端；凭据仍只在服务器 `/opt/traceable-support/provider.env`（0600），预算仍为日 ¥20 / 月 ¥100 / 次 ¥1、自动重试 0 |
| 下一检查点 | 若用户批准后续同步，则只合并 `main@8a306165` 到 PR #55 分支、解决 `docs/status.md` 当前事实冲突并重新跑 Checks；本次不合并 PR #55 |

## 当前队列

| Task | 状态 | 候选 / 结果 |
| --- | --- | --- |
| `retrieval-badcase-loop` | `draft_pr_conflicted` | Draft PR #55，head `45db89fee48c6534e4559d32b3134ba5704ce855`；目标来源 BM25 第 6 → 第 5，BM25 Top-5 14/16 → 15/16；当前仅 `docs/status.md` 与 main 冲突，尚无 PR workflow run，未合并 |
| `portfolio-evidence-first-screen` | `delivered` | PR #54 head `d8cd2081f5b481cbef597ba5dd534f78d70851b4` squash merge 为 `8a306165221387805ee33e6c20b45d9260c48658`；main CI 成功，部署链预检成功、deploy 跳过，运行产品未改变 |
| `frontend-polish-delivery` | `delivered` | PR #52 合并为 `915ca4e`；main CI、生产部署、公网完整 SHA、四页文案与样式检查通过；Provider 调用 0，工作记录已归档 |
| `frontend-polish` | `delivered` | 本地候选 `a6ff775` 经后续最小治理记录形成 PR #52 并公开交付；用户于 `2026-08-01` 完成整体体验验收 |
| `portfolio-guided-path` | `accepted_local` | `14e4c6b`；33 项测试、lint、typecheck、build 通过，桌面主路径与折叠入口已由用户本地验收；未推送、未部署 |
| `release-sha-health` | `delivered` | PR #50 合并部署；公网健康返回与 `66af626ba4debf4c8a1cf91da023754168c5b908` 精确一致的完整 `release_sha` |
| `ISSUE-29-PORTFOLIO-PRESENTATION` | `delivered` | PR #43 合并为 `8da0546` 并自动部署；GitHub、真实运行 GIF、公网站点与当前事实统一，用户验收 `PASS` |
| `TASK-TRACEABLE-LIVE-WORKBENCH` | `delivered` | PR #31 合并部署，最终生产体验验收 `PASS`；首页一致性由 PR #34 修正；Issue #28 已关闭归档 |
| `TASK-REAL-RUN-EVIDENCE` | `delivered` | 内容随 PR #38 合入 main（#38 叠加绑定式溯源门改造 ADR-0007 与部署链路解锁；PR #37 经两轮定向复核全绿后关闭、未直接合并）；真实 Provider live 已上线（`766ba3f`）；工作记录归档 `docs/work/completed/real-run-evidence/` |

Task 可以并行；required Checks、所需当次授权、触发时的评审兜底、受保护 `main`、部署和
用户验收仍按依赖串行。

## 2026-08-01 派发交付回执：README 首屏量化证据

- 外部交付：按用户当次明确授权创建分支 `codex/portfolio-evidence-first-screen` 与 Draft PR
  [#54](https://github.com/suuny-ab/traceable-support-agent/pull/54)，base 为 `main`；README 提交为
  `54c5abd896a57cee3fb41881fe72c9bdba089289`，首次状态提交为
  `fbcb268f7cb18f62ae3e140c11418ca8883f36cf`。
- PR 门：最终 head `d8cd2081f5b481cbef597ba5dd534f78d70851b4` 的运行 `30700889472`
  已成功；governance、web、api、containers 四个 required jobs 均为 completed / success。
  web、api、containers 的运行时步骤因 `governance_only` 跳过，因此这些绿灯不证明运行时行为；
  publish job 跳过。
- 本地验证：在 `origin/main + README/docs/status` 的干净快照上，
  `python tools/check_public_repo.py --scope index` 通过（214 files、8 public cases）；检索体检
  通过（16 cases、Provider 调用 0）；检索 / Stage 12 定向测试 `16 passed`。
- 工作区边界：直接扫描当前工作树会被范围外的 `AGENTS.md` 本机路径与未跟踪 `clean/` 缓存
  拦截，因此没有把该失败冒充候选失败，也没有修改或提交这些范围外内容。
- 通道回执：本执行环境的 `.git` 只读，终端访问 GitHub 被阻断，`gh` 本地令牌也已失效；
  远端内容通过已登录 GitHub 页面从本地 README / 状态文件逐字交付。本地工作文件保持同一内容，
  但 Git 分支元数据需在具备正常 Git 权限的会话中再 `fetch` 同步。

- 指标块：RRF Top-5 必需来源覆盖 `16/16`、错误型号来源 `0`；边界明确为 16 个冻结公开
  合成开发题的来源覆盖，不写回答质量、线上成功率或未见集结论。
- 自动化证据：部署候选 `915ca4e` 的 main CI `30690110223` 五个 job 全部成功；API 日志为
  `137 passed / 2 skipped`，Stage 12 runner 为 `13 passed`。派发中的旧数字 `132` 未写入。
- Stage 12：README 保留原始 `19/24`、`9` 通过观测，说明 Issue #21 已修复已知边界机制但
  未重跑未见集，并把新的验证说明卡、另行授权和 Issue #14 发布判断列为条件式下一步。
- 链接核验：Issue #21 为 `CLOSED`；main CI 结论为 `success`；公网 health 返回
  `status=ok`、`live_experience=available`、`release_sha=915ca4ef7820870ee42fbef69ea719498d7f402d`。
- 本次派发只修改 README 和状态回执；工作树中的 `AGENTS.md` 属单独批准的规则变更，不计入
  本派发。Provider 调用 `0`、产品运行 `0`，未修改产品代码、评测本体、检索、凭据、预算或部署。

## 2026-08-02 授权执行回执：PR #54 合并与 Draft PR #55

- 授权来源：用户在 Git 外 `派发/授权请求.md` 裁决节以“全批”明文批准：先合并 PR #54
  精确 head `d8cd2081f5b481cbef597ba5dd534f78d70851b4` 并允许既有自动部署链，再推送
  `codex/retrieval-badcase-loop` 的 `80953b5` / `45db89f` 并创建 Draft PR；第二项不含合并。
- PR #54：先由 Draft 转 Ready，再用 `expected_head_sha=d8cd208…` 执行 squash merge；GitHub
  返回 `merged=true`，main 提交为 `8a306165221387805ee33e6c20b45d9260c48658`。
- main CI：`ci-release` 运行 `30738245420`，event `push`，head 为完整 `8a306165…`，结论
  `success`。
- 自动部署链：运行 `30738264198` 结论 `success`；preflight 成功，deploy job 因文档候选
  `governance_only` 为 `skipped`。这证明链路按发布决定执行，不表示运行产品部署了 `8a306165`。
- 公网健康：`status=ok`、`live_experience=available`、
  `release_sha=915ca4ef7820870ee42fbef69ea719498d7f402d`；与 deploy 跳过一致，运行产品版本未改变。
- 检索分支：推送后远端 `refs/heads/codex/retrieval-badcase-loop` 与批准 head
  `45db89fee48c6534e4559d32b3134ba5704ce855` 精确一致；创建 Draft PR
  [#55](https://github.com/suuny-ab/traceable-support-agent/pull/55)，base `main@8a306165`，6 个变更
  文件、未合并。
- PR #55 当前边界：GitHub 报 `mergeable=false` 且尚无 workflow run；只读 `git merge-tree`
  确认冲突仅为双方都更新的 `docs/status.md`。本次没有改写或追加已批准 head，后续同步需形成
  新候选和新授权。

## 2026-08-02 派发交付回执：检索 badcase 最小闭环

- 开工三问：从 16 题公开开发集选一个组件级 badcase，做一处检索改进并保存前后证据；不加
  知识接入、新集成或新页面，不改冻结问题、标签或知识内容，不调用 Provider，不推送、建 PR、
  合并或部署；完成线为基线/候选可重算、相关测试与治理通过、证据及边界落盘。
- 候选位置：隔离分支 `codex/retrieval-badcase-loop`；实现提交 `80953b5`，状态提交
  `45db89f`；已按授权推送并创建 Draft PR #55，未合并。
- badcase：`RET-DEV-R2-008` 的必需来源 `COMMON-FAQ/wet-environment` 在基线 BM25 排第 6，
  漏出 Top-5；RRF 基线已覆盖该题，所以本切片只改善词面召回韧性，不扩大产品结论。
- 一处改进：产品 BM25 候选在查询和候选文本同时增加显式 `domain:liquid-ingress` 等价标记，
  覆盖“吸进水 / 吸入液体 / 吸取液体 / 进水 / 进液”；基础 `tokenize`、冻结题、标签和知识
  内容不变。
- 证据资产：第一次冻结结果 `web/app/lib/retrieval-checkup-v1.json` 保持不变；本地候选写入
  `evals/retrieval-badcase-candidate-v1.json`，并标明它是对同一公开开发集的定向调优，不是
  未见证据。
- 前后结果：目标来源 BM25 第 6 → 第 5；BM25 Top-5 14/16 → 15/16；BGE 保持 14/16；
  RRF 保持 16/16；三路 Top-10 均 16/16；错误型号来源均 0；Provider 调用 0。
- 验证：baseline 与 product_candidate 两个 retrieval check 均通过（各 16 cases）；检索定向
  测试 4 通过；API 全集 138 通过 / 2 跳过；Stage 12 runner 13 通过；`check_public_repo
  --scope worktree` 通过（215 files、8 public cases）；`git diff --check` 通过。
- 证据边界：测试使用主工作区已有且通过 manifest 校验的本地 BGE 文件；没有下载模型或调用
  Provider。本地绿不是远端 CI 绿，开发集改善不证明生成质量、线上成功率、未见集或发布质量。

## 当前产品事实

- 方向 B 的公开体验位于 <https://47.84.34.86/>；`2026-07-29` 起为真实 Provider live
  模式。当前生产运行的产品发布为 `66af626ba4debf4c8a1cf91da023754168c5b908`，
  公网健康同时返回该完整 `release_sha` 与 `live_experience=available`；后续仅文档收口不触发部署。
- 生成门语义：`2026-07-29` 起为绑定式溯源（ADR-0007）：每条结论必须绑定真实存在的
  证据 / 义务 ID，证据原文随回答展示；不再要求措辞逐字复现原文。真实 Provider 本地
  复测过门率 0/2 → 3/3，公网首跑 1/1 `completed`。
- 此前求职交付为已部署并通过最终体验验收的公开 Beta 回放版（部署 `34079d7`）；真实
  Provider、凭据和费用在该阶段均未启用。
- `product/0.1.0` 尚未发布；Stage 12 已执行一次（19/24、9 通过），Issue #21 已修复当时的
  两条边界缺陷但未重跑未见集；Issue #22 已以部分结果和已知限制收口，不形成成功率主张。
- Issue #28 已于 2026-07-28 以 `not planned` 关闭：在该关闭时点，视觉、公开回放体验与
  生产验收部分已完成，公开真实 Provider 尚未启用；该历史结论已由 2026-07-29 的独立
  授权 live 上线增量更新，不能再作为当前公网状态。
- Issue #29 已完成：PR #43 合并部署后，GitHub 与公网展示、真实运行证据、工程边界和复现
  入口已统一，并于 `2026-07-30` 通过用户面试官视角验收；Issue #14 发布或保持 Beta 判断
  继续后置。
- 当前工作树和公开 GitHub 仓库是产品唯一权威开发来源。
- 唯一权威开发位置为主 worktree `traceable-support-agent`；
  `traceable-support-agent-live-workbench`、`traceable-support-agent-live-integration` 与
  `traceable-support-agent-ci-contract` 已退出当前事实来源，物理移除仍需单独授权。
- 收敛前主 worktree 未提交修改完整保存在本地分支 `backup/portfolio-experience-wip-20260726`
  （`a683dff`），可用 `git restore --source=backup/portfolio-experience-wip-20260726 .` 恢复。
- 旧仓和临时回滚材料不是权威来源；删除仍是未经授权的独立破坏性动作。
- 受保护 `main` 的 CI 全绿后自动进入既有生产部署流程；失败部署不自动重试。

## 权威来源

- 产品事实：`PROJECT.md`
- 结果路线：`ROADMAP.md`
- 最近完成记录：`docs/work/completed/release-sha-health/`、`docs/work/completed/portfolio-live-experience/`、`docs/work/completed/ci-proof-contract/`、`docs/work/completed/real-run-evidence/`
- 工程规则：`docs/engineering/`
- Agent 协作规则：`docs/engineering/agent-workflow.md`

通信链路测试回执 2026-08-01 21:31:45 +08:00

## 2026-08-02 派发回执：开发治理只读定位

- 已完成 `AGENTS.md` 与 `docs/engineering/` 9 份文件的全量盘点，并新增
  `docs/work/governance-audit-20260802.md`；审计只列问题与证据，不含治理方案。
- 近 7 天 38 个提交中 30 个触碰 `docs/status.md`，累计 770 行增删；PR #55 的唯一冲突也是
  `docs/status.md`。审计确认状态职责叠加、四文件机器门、规则扫描自冲突和历史/当前混写是主要损失源。
- 未修改产品、规则、代码、测试或 Workflow，未处理 PR #55 冲突，未调用 Provider、未推送、
  未合并、未部署；除本回执外，既有工作区改动保持原样。
```

## 2026-08-02 外部事实续记

- PR #55 冲突解锁后最终 head 为 `9ce20c2498a28f774405894f8a0adc8783165ea0`；PR 保持 Draft / OPEN，GitHub 报 `MERGEABLE`、`CLEAN`，未合并。
- PR #55 的 `ci-release` run `30739667475` 已完成且结论 `success`；governance、web、api、containers 四个 required jobs 均为 `success`，publish 为 `skipped`。
- 公网健康核验仍为 `status=ok`、`live_experience=available`、`release_sha=915ca4ef7820870ee42fbef69ea719498d7f402d`；这证明运行身份未随 Draft PR 改变。
- `docs/work/governance-audit-20260802.md` 以主工作区原文件 byte-for-byte 保留：13,783 bytes，SHA-256 `d0f0331658cf4ab1d78f7481bbc62212e143d7bac221cc898fa4f2f66bb933f5`。

## 2026-08-02 状态拆层本地验证

- 当前层 `docs/status.md` 为 30 行；从本日志代码块反解出的迁移快照为 14,529 bytes，SHA-256 与迁移源 `6deb0fed42a65b86cca0b2c75b289ae8fe42d0e6190b01de1f2ae41b3e5b9e1c` 精确一致。
- `AGENTS.md` 与 `docs/engineering/github-lifecycle.md` 中每一处 `docs/status.md` 引用均同时给出只追加历史层 `docs/status-log/YYYY-MM.md`；未修改其他规则文件。
- `python tools/check_public_repo.py --scope worktree` 通过：216 files、8 public cases；`git diff --check` 通过。
- 显式绑定现场已有且经清单校验的本地 BGE 根目录后，治理工具测试 108 个通过、8 个跳过；Fast API 为 56 passed、24 subtests passed。首次未绑定模型根目录的工具测试在 `embedding_model_file_inventory_invalid` 停止，复跑已消除环境缺口。
- 本切片 Provider 调用 0、产品运行 0；未修改产品代码、CI / Workflow、公开主张、凭据、预算、安全边界或部署。

## 2026-08-02 状态拆层 Draft PR 回执

- 本地候选提交 `6228c73` 已推送到 `codex/status-two-layer`，并创建 Draft PR [#56](https://github.com/suuny-ab/traceable-support-agent/pull/56)，base 为 `main`；未转 Ready、未合并、未部署。
- 当前层随本条只追加回执更新为 PR #56；本回执提交推送后触发同一 head 的 required Checks，最终运行身份由 GitHub PR / Checks 实时状态核验。

## 2026-08-02 PR #55 冲突解锁候选

- 授权：用户批准同步最新 main、按两层状态重建回执、复跑检查并推送新 head；若该 head 的 required Checks 全绿，继续 squash merge、既有自动部署链和公网健康核验。任一步失败立即停止。
- 同步：`codex/retrieval-badcase-loop` 从旧 head `9ce20c2498a28f774405894f8a0adc8783165ea0` 合入 `origin/main@6c31d912b96f2f24d3e47eb8d93ecaf755f4ed3e`；唯一冲突为 `docs/status.md`，先以 main 30 行当前层整文件字节级恢复，再重建本候选回执；月度日志只从末尾追加。
- 检索证据保持：baseline 与 product_candidate 各 16 cases 通过，Provider 调用 0；目标来源 BM25 第 6 → 第 5，BM25 Top-5 14/16 → 15/16，BGE 14/16、RRF 16/16、三路 Top-10 16/16、错误型号来源 0。它仍是同一公开合成开发集的定向改善，不是未见集、回答质量或发布结论。
- 本地验证：检索定向测试 4 passed；API 全集 138 passed / 2 skipped、24 subtests passed；治理工具 108 passed / 8 skipped；`check_public_repo --scope worktree` 通过（217 files、8 public cases）。
- 边界：没有改动检索代码、评测资产、产品行为、Provider、凭据、预算或安全边界；本次同步只吸收 main 的状态两层治理增量并重建当前回执。

## 2026-08-02 PR #56 合并、部署与公网核验

- 授权：用户明文批准 squash merge PR #56 精确 head `baf3c2c38e432e44c3a8d843a073ca5d6bc83a79`，并允许进入既有自动部署链；未扩展到 PR #55、Issue #14 或其他外部写入。
- 合并：GitHub 以 `sha` 前置条件接受 squash merge，PR #56 状态为 `MERGED`，main 提交为 `6c31d912b96f2f24d3e47eb8d93ecaf755f4ed3e`。
- main CI：`ci-release` run `30741231900` 在完整 merge SHA 上完成且结论 `success`；governance、web、api、containers 与 publish 全部成功。
- 自动部署链：`deploy-production` run `30741304973` 由上述 main CI 的 `workflow_run` 触发；preflight 与实际 deploy 均为 `success`，部署包已通过严格主机验证并激活。
- 公网健康：部署后 `/api/v1/health` 返回 `status=ok`、`live_experience=available`、`release_sha=6c31d912b96f2f24d3e47eb8d93ecaf755f4ed3e`，与 main / 部署清单精确一致。
- 边界：本任务 Provider 调用 0；未修改凭据、预算、安全边界、评测结论或发布标签。CI 注释仅为 GitHub Actions 的 Node.js 20 弃用提醒，不影响本次成功结论。

## 2026-08-02 PR #55 精确合并阻断

- 授权：用户批准 squash merge PR #55 精确 head `9ce20c2498a28f774405894f8a0adc8783165ea0` 并允许进入既有自动部署链；授权不包含改写该 head。
- 合并前复核：head 未漂移，PR 仍为 Draft / OPEN；该 head 的 `ci-release` run `30739667475` 已成功，四个 required jobs 全绿、publish 跳过。
- 新阻断：PR #56 合入 `main@6c31d912b96f2f24d3e47eb8d93ecaf755f4ed3e` 后，GitHub 将 PR #55 判为 `CONFLICTING / DIRTY`；只读 `git merge-tree` 以 merge base `8a306165…` 复算，唯一冲突为 `docs/status.md`。
- 停止线：未转 Ready、未调用 merge API、未更新 PR 分支，因而没有产生 PR #55 的 main CI、部署运行或产品变更；公网仍为 `status=ok`、`live_experience=available`、`release_sha=6c31d912b96f2f24d3e47eb8d93ecaf755f4ed3e`。
- 下一授权：需先允许同步最新 main、以 main 当前两层结构重建状态、复跑检查并推送新 head；该新 head 的合并与自动部署必须重新绑定精确值。

## 2026-08-02 PR #55 冲突解锁、合并与部署

- 全链路授权：用户批准同步最新 main、以 main 两层状态重建、复跑检查并推送新 head；若该 head 的 required Checks 全绿，继续 squash merge、自动部署链与公网健康核验，任一步失败立即停止。
- 冲突解锁：分支合入 `main@6c31d912b96f2f24d3e47eb8d93ecaf755f4ed3e`，唯一冲突 `docs/status.md` 先恢复为 main 整文件再重建回执；新 head 为 `b37b893e5bd179816a5e59cc94371ea1b809eebc`，相对 main 仅 7 个预期文件。
- 本地证据：baseline / product_candidate 各 16 cases、Provider 调用 0；检索定向 4 passed；API 138 passed / 2 skipped、24 subtests passed；治理工具 108 passed / 8 skipped；公开仓扫描 217 files / 8 public cases。
- 候选门：新 head 的 `ci-release` run `30742061127` 成功，governance、web、api、containers 四个 required jobs 全绿，publish 按 PR 语义跳过；GitHub 报 `MERGEABLE` 后才转 Ready。
- 合并：GitHub 以新 head 作为 `sha` 前置条件完成 squash merge，PR #55 状态为 `MERGED`，main 提交为 `1be50a899fbeeae2203cc8dee12125d3811c186a`。
- main CI 与部署：`ci-release` run `30742129018` 的 governance、web、api、containers、publish 全部成功；`deploy-production` run `30742201142` 的 preflight 与实际 deploy 均成功，部署包已激活。
- 公网健康：`/api/v1/health` 返回 `status=ok`、`live_experience=available`、`release_sha=1be50a899fbeeae2203cc8dee12125d3811c186a`，与 main / 部署清单精确一致。
- 边界：本任务 Provider 调用 0；未修改凭据、预算、安全边界、冻结问题、标签或知识内容。BM25 Top-5 14/16 → 15/16、RRF 16/16 仍仅是同一公开合成开发集来源覆盖结果。

## 2026-08-02 治理规则瘦身候选

- 范围：从 `origin/main@1be50a899fbeeae2203cc8dee12125d3811c186a` 建立隔离分支 `codex/governance-rule-slimming`；仅修改规则索引、工程规则、治理扫描 / 测试和两层状态，不改产品代码、Workflow、Provider、凭据、预算、安全边界或公开产品主张。
- 规则结果：`AGENTS.md` 从 167 行收至 67 行并改用可提交的便携派发指针；授权默认值全文只保留在 `docs/engineering/review.md`，其他活动规则改为链接。
- 机器合同：移除 `state != ready` 对活动目录数量的错误推断；活动目录仍须精确四文件并由状态链接。PR #43 `in_progress`、PR #52 `candidate`、检索 `active` 三个无目录回归均通过。
- 本地验证：治理工具 109 passed / 8 skipped；API 138 passed / 2 skipped、24 subtests passed；Web lint、typecheck、生产构建与 36 tests 通过；`check_public_repo --scope worktree` 与 `git diff --check` 通过。
- 外部事实：PR #55 已合并为 `1be50a899fbeeae2203cc8dee12125d3811c186a`，main CI `30742129018` 与部署 run `30742201142` 均成功。本轮三个直连客户端在 TLS 握手阶段失败，按规则停止重试；不据此推导公网产品故障，最近成功健康回执仍为该完整 SHA 与 `live_experience=available`。
- 授权 / 停止线：用户仅批准本分支一次推送和创建 Draft PR；转 Ready、合并及由此进入既有自动部署链均未授权，PR 建立后在 Git 外写精确 head 授权请求并等待。

## 2026-08-02 PR #57 合并、部署与公网核验

- 授权：用户明文批准 squash merge PR #57 精确 head `bddb35b754325194c25e5b30739004e26f263520`，并允许进入既有自动部署链；未扩展到新规则、产品、Provider 或其他外部动作。
- 合并：PR #57 在 head 未漂移且 required Checks 全绿后转 Ready；GitHub 以 expected-head 前置条件完成 squash merge，PR 状态为 `MERGED`，main 提交为 `3c981c6d2c1aa711048b8638e78041ff4cd7ae50`。
- main CI：`ci-release` run `30743200450` 在完整 merge SHA 上完成且结论 `success`；governance、web、api、containers 与 publish 全部成功。
- 自动部署链：`deploy-production` run `30743280611` 由上述 main CI 的 `workflow_run` 触发；preflight 与实际 deploy 均为 `success`，部署包已通过严格主机验证并激活。
- 公网健康：正式地址 `https://47.84.34.86/api/v1/health` 返回 `status=ok`、`live_experience=available`、`release_sha=3c981c6d2c1aa711048b8638e78041ff4cd7ae50`，与 main / 部署清单精确一致。
- 边界：本任务 Provider 调用 0；未修改产品代码、Workflow、凭据、预算、安全边界、评测结论或公开产品主张。CI 注释仅为 GitHub Actions 的 Node.js 20 弃用提醒，不影响成功结论。

## 2026-08-02 文档园丁候选

- 范围：从 `origin/main@3c981c6d2c1aa711048b8638e78041ff4cd7ae50` 建立隔离分支 `codex/doc-gardener`；只新增只读扫描工具、测试、首次报告和非阻断检查入口，并修复机器可证明的文档腐坏，不改产品代码、生产、Provider、凭据、预算或安全边界。
- 扫描合同：园丁从 `PROJECT.md` 与 `docs/status.md` 读取 canonical release / live / product / state，扫描 18 个活动文档；默认只告警且退出成功，`--fail-on` 仅供治理专项使用，工具本身不修改文档。
- 首次结果：修复前 `stale=2 / review=1`；README 的旧 `release_sha=915ca4e…` 改为实时健康入口，`PUBLIC_CONTEXT.md` 把已交付展示改回已交付事实；迁移记录中缺少时间锚的“当前产品合同”保持 `review=1`，未擅改。修复后 `stale=0 / review=1`，详见 `docs/work/doc-gardener-initial-report-20260802.md`。
- 检查接入：`ci-release` governance job 新增 `continue-on-error: true` 的 advisory step；`docs/engineering/quality.md` 同步登记本地命令，文档 finding 不阻断构建。
- 本地验证：治理工具 113 passed / 8 skipped；API 138 passed / 2 skipped、24 subtests passed；Web lint、typecheck、生产构建与 36 tests 通过；`check_public_repo --scope worktree` 与 `git diff --check` 通过。
- 授权 / 停止线：用户仅批准本分支一次推送和创建 Draft PR；转 Ready、合并及由此进入既有自动部署链均未授权，PR 建立后在 Git 外写精确 head 授权请求并等待。

## 2026-08-02 PR #58 合并、部署与公网核验

- 授权：用户明文批准 PR #58 转 Ready、squash merge 精确 head `3f74453612f293c75a483edb9db7b219333de22b`，并允许进入既有自动部署链；未扩展到新产品、Provider、凭据、预算或其他外部动作。
- 合并：PR #58 在 head 未漂移且 required Checks 全绿后转 Ready；GitHub 以 expected-head 前置条件完成 squash merge，PR 状态为 `MERGED`，main 提交为 `31541cfaca4f71c507fbda6f4774ed8e7c8b4a7f`。
- main CI：`ci-release` run `30744313617` 在完整 merge SHA 上完成且结论 `success`；governance、web、api、containers 与 publish 全部成功。
- 自动部署链：`deploy-production` run `30744389288` 由上述 main CI 的 `workflow_run` 触发；preflight 与实际 deploy 均为 `success`，部署包已通过严格主机验证并激活。
- 公网健康：正式地址 `https://47.84.34.86/api/v1/health` 返回 `status=ok`、`live_experience=available`、`release_sha=31541cfaca4f71c507fbda6f4774ed8e7c8b4a7f`，与 main / 部署清单精确一致。
- 边界：本任务 Provider 调用 0；未修改产品行为、凭据、预算、安全边界、评测结论或发布标签。

## 2026-08-02 依赖漂移分析候选

- 范围：从 `origin/main@31541cfaca4f71c507fbda6f4774ed8e7c8b4a7f` 建立隔离分支 `codex/dependency-drift`；只新增依赖漂移报告并更新两层状态，不修改依赖、锁文件、代码、生产或 Provider。
- 完整锁审计：Python 三份锁分别解析 13 / 34 / 5 包、去重 46 包，钉定 `pip-audit==2.10.1` 均未发现已知漏洞；Web lock 共 436 个依赖 entry，完整闭包为 high 3 / critical 0，production-only 为 high 2 / critical 0。
- 安全结论：Web 的 3 个 high 包 finding 对应 2 个唯一 advisory；`postcss==8.5.14` 位于生产闭包，`brace-expansion==1.1.16 / 5.0.7` 位于开发 / 构建闭包。报告建议另立只做安全 patch 的升级切片，本任务不执行修复。
- 漂移结论：直接依赖没有因安全公告而必须跨 major；Python 同主版本漂移与 Web patch / minor 漂移归为可观察，ESLint 10、TypeScript 7、`@types/node` 运行时对齐后置独立验证。
- 数据来源：官方 PyPI JSON、npm registry、GitHub Advisory Database；两次 npm audit 前后 `package-lock.json` SHA-256 未变化。Provider 调用 0，公网与生产写入 0。
- 声明一致性：Python base / live 与各自需求源一致，Web 根声明与 lock 根 entry 一致；Python test extra 仍为 `pytest==9.0.2`，而 `requirements-test.txt/.lock` 为 `9.0.3`，报告建议另立声明同步切片，本任务不修改。
- 本地验证：治理工具 113 passed / 8 skipped；API 138 passed / 2 skipped、24 subtests；Web clean install、lint、typecheck、production build 与 36 tests 全部通过；依赖与锁文件仍为零差异。

## 2026-08-02 PR #59 合并、部署与公网核验

- 授权：用户明文批准 PR #59 转 Ready、squash merge 精确 head `c1f6c93b7474bfa310591a20aed2308cc3475817`，并允许进入既有自动部署链；未扩展到报告所建议的依赖修复。
- 合并：PR #59 以精确 head 前置条件 squash merge，main 提交为 `3636d24fc858d27ad0b4e35d8732650444f4ccdd`。
- main CI：`ci-release` run `30745848617` 在完整 merge SHA 上完成且结论 `success`；governance、web、api、containers 与 publish 全部成功。
- 自动部署链：`deploy-production` run `30745943189` 由上述 main CI 触发；preflight 与实际 deploy 均为 `success`。
- 公网健康：`/api/v1/health` 返回 `status=ok`、`live_experience=available`、`release_sha=3636d24fc858d27ad0b4e35d8732650444f4ccdd`，与 main / 部署清单精确一致。
- 边界：本任务 Provider 调用 0；未在该合并任务中修复依赖、改变产品行为、凭据、预算、安全边界或发布标签。

## 2026-08-02 依赖安全补丁与 Python 声明同步候选

- 范围：从 `origin/main@3636d24fc858d27ad0b4e35d8732650444f4ccdd` 建立隔离分支 `codex/dependency-security-maintenance`；只修复报告命中的 Web advisory、同步 pytest 声明并增加防漂移机器检查，不改产品代码、生产或 Provider 配置。
- Web 修复：显式 override `postcss` 从 `8.5.14` 提升到最低安全版 `8.5.18`；两份满足现有父依赖范围的 `brace-expansion` 仅刷新为 `1.1.18` / `5.0.9`，没有带入 Next、React、Tailwind、ESLint 或 TypeScript 升级。
- Python 同步：`pyproject.toml` test extra 从 `pytest==9.0.2` 对齐为需求源 / 锁已使用的 `9.0.3`；`check_public_repo` 新增声明一致性失败门及回归测试。
- 安全证据：完整与 `--omit=dev` 的 npm audit 均为 0 vulnerabilities；钉定 `pip-audit==2.10.1 --disable-pip --no-deps` 对 base、live、test 三份锁均为 0 个已知漏洞。
- 本地验证：模型缓存 7 个文件通过大小与 SHA-256 只读清单校验；治理工具 114 passed / 8 skipped，API 138 passed / 2 skipped、24 subtests，Web clean install、lint、typecheck、production build 与 36 tests 全部通过；公开仓扫描和 `git diff --check` 通过。
- 授权 / 停止线：用户仅批准本分支推送与创建 Draft PR；转 Ready、合并及由此进入既有自动部署链均未授权，PR 建成并确认 Checks 后在 Git 外写精确 head 授权请求并等待。

## 2026-08-02 PR #60 合并、部署与公网核验

- 授权：用户明文批准 PR #60 转 Ready、squash merge 精确 head
  `9107b91228cf4da071fe230f670987783a738826`，并允许进入既有自动部署链。
- 合并：PR #60 合并为 `main@db72aa0e3b31ad749ba1f991ecd5b2ca4c1fa949`；main CI
  `30747284056` 与自动部署 `30747449697` 均成功。
- 公网健康：`/api/v1/health` 返回 `status=ok`、`live_experience=available`、
  `release_sha=db72aa0e3b31ad749ba1f991ecd5b2ca4c1fa949`，与 main / 部署身份一致。
- 边界：本合并 Provider 调用 0；未扩展到其他依赖升级、预算、凭据或发布标签。

## 2026-08-02 pgvector 生产接入候选

- 范围：从 `origin/main@db72aa0e3b31ad749ba1f991ecd5b2ca4c1fa949` 建立隔离分支
  `codex/pgvector-production-integration`；只增加版本化向量 schema readiness、运行期内存
  fallback、内部生产 pgvector 编排、发布 / 回滚门、对比证据和两层状态，不新增知识或调参。
- readiness：DSN、连接、`vector` 扩展、migration v1、`vector(512)`、复合主键、HNSW cosine
  索引和 `SELECT 1` 全过才启用；同步 / 查询断线时同一请求使用已计算的内存向量，模型或输入
  错误不被掩盖。公开 health 四字段、Provider、预算、凭据和人工批准合同未改。
- 16 题结果：内存 / pgvector 的 Top-5 与 Top-10 均为 16/16，错误型号来源 0，Top-10 逐题
  一致；本机冷启动 2035.846 / 1955.757ms，热态 16 题中位数 192.534 / 580.385ms。固定
  执行顺序和公开开发集不能形成跨机性能、未见集、生成质量或线上成功率主张。
- 生产边界：pgvector 镜像固定 digest，数据库不发布 5432；数据 / 随机口令分卷持久化，API
  UID 只读 `.pgpass`。API 不依赖数据库启动，但候选 / 回滚版本只有在公开 health 与 pgvector
  readiness 都过门后才提交发布锚点；部署和回滚不删除卷。
- 本地证据：真库 6 passed；API 148 passed / 24 subtests；live 容器在健康 / 不可达数据库下
  分别选择 pgvector / memory fallback，四字段 health 不变，离线检索 8 cases / Provider 0；
  治理 114 passed / 8 skipped；Web clean install、lint、typecheck、build 与 36 tests 通过；
  公开仓、Workflow YAML、release shell syntax 和差异检查通过。
- Docker 边界：本地 build 客户端在 304 秒停止线超时但 BuildKit 随后产出非 root live 镜像，
  上述容器检查在该镜像实际通过；干净构建仍以 Draft PR 的 Linux containers check 为准。
- 授权 / 停止线：用户批准本分支推送与创建 Draft PR；转 Ready、合并、自动部署、生产状态
  修改和 Provider 调用均未授权，required Checks 后只提交精确 head 授权请求并停止。

## 2026-08-02 pgvector Draft PR 回执

- 实现候选 `e3d059a` 已推送并创建 Draft PR
  [#61](https://github.com/suuny-ab/traceable-support-agent/pull/61)，base 为 `main`；首次 GraphQL
  创建请求 EOF 且远端确认未生成 PR，原参数单次重试后成功，没有重复 PR。
- 本条只把 PR 身份回写当前层与只追加日志；最终 head 以本回执提交为准，required Checks
  必须在最终 head 全绿后才提交合并授权请求。
- PR 保持 Draft；未转 Ready、未合并、未部署、未修改生产状态，Provider 调用 0。

## 2026-08-02 PR #61 合并、部署与公网核验

- 授权：用户明文批准 PR #61 精确 head `074bab2bb00268957321da40348a58fca1b82797`
  转 Ready、squash merge 并进入既有自动部署链；未扩展到 Provider 或其他候选。
- 合并与 CI：PR #61 合并为 `main@8ca825d58d2b42fdaecfb59e0ca6a0ade45d6f24`；main CI
  `30750319850` 的 governance、web、api、containers、publish 全部成功。
- 部署与公网：`deploy-production` run `30750414782` 的 preflight 与实际 deploy 成功；公网
  `/api/v1/health` 返回同一完整 `release_sha`、`status=ok`、`live_experience=available`。
- 边界：Provider 调用 0；公网 health 不能单独证明每个请求实际选择的检索后端。工作记录随
  下一候选从 `docs/work/active/` 原样归档到 `docs/work/completed/`。

## 2026-08-02 P1 最小观测面本地候选

- 范围：从 `origin/main@8ca825d58d2b42fdaecfb59e0ca6a0ade45d6f24` 建立集成分支
  `night-20260802`；只增加现有 API 进程内的 SQLite 聚合观测、只读端点、失败降级、合同测试
  和两层状态，不新增服务、依赖、外部监控或 Provider 行为。
- 数据边界：只按规范化路由 ID / 方法 / 错误分类累计请求数和耗时；不保存原始路径、查询串、
  请求体、Cookie、IP 或异常文本。观测端点自身不计数，写入异常不改变原请求响应。
- 本地证据：定向 API 19 passed；API 全集 147 passed / 4 skipped / 24 subtests；治理工具
  114 passed / 8 skipped；公开仓 233 files / 8 public cases；园丁 stale 0 / review 1；Provider
  调用 0。提交、推送与 Draft PR 尚待执行。
- 授权 / 停止线：用户预授权推送 `night-20260802` 和创建 Draft PR；不转 Ready、不合并、
  不部署，不修改生产状态、Provider、预算或凭据。

## 2026-08-02 P1 最小观测面 Draft PR 回执

- 实现提交 `44de2a4d1dccfd8cbc856e5b877a540bca6af20b` 已推送到集成分支
  `night-20260802`，并创建 Draft PR [#62](https://github.com/suuny-ab/traceable-support-agent/pull/62)，
  base 为 `main@8ca825d58d2b42fdaecfb59e0ca6a0ade45d6f24`。
- PR 明确保持 Draft；未转 Ready、未合并、未部署、未修改生产状态，Provider 调用 0。
- 本条身份回执提交后会形成最终 head；required Checks 必须绑定该最终 head 启动，实时结论以
  GitHub 为准。本任务只确认启动，不把排队或运行中写成通过。

## 2026-08-02 公开合成检索未见集首次观测

- 冻结资产：`evals/retrieval-holdout-v1.json` 含 10 题（CZ-R1 / CZ-R2 各 5）与 16 个不进入
  生产语料的独立合成知识单元；前置提交 `6e57c5e229af01f4949df9c99d6ec6bdf03af74a`，题集
  SHA-256 `7e3163b26062615440a49825d2d10d0409890d026fc82b8387b7a6f4c247afb6`。
- 零重复门：相对既有 16 题开发集，问题规范化全文、来源引用、生产知识规范化全文重复均为
  0；16 个评测知识单元全部被同型号标签覆盖。
- 首次观察：BM25、Local BGE、RRF 的 Top-5 / Top-10 全标签覆盖均为 10/10，Top-10 错误型号
  来源均为 0；逐题名次已落盘。Provider 调用 0、generation 调用 0，未改生产检索或语料。
- 执行说明：首次命令在建索引前因隔离 worktree 缺默认模型缓存而失败，没有排名或输出；主仓
  既有 7 文件缓存经清单大小 / SHA-256 核验后显式复用，随后完成第一次实际排名，无下载、
  换模或排名重试。
- 结论边界：只证明冻结公开合成集的检索来源覆盖与型号过滤观察值，不证明回答质量、线上
  成功率、真实泛化或发布成熟度；揭示后该版本只作回归记录，禁止对着结果调参或改资产。
- 本地验证：API 149 passed / 4 skipped / 24 subtests；治理工具 114 passed / 8 skipped；公开仓
  241 files / 8 public cases；园丁 stale 0 / review 1；差异检查通过。新增测试只核对冻结结构和
  已落盘回执，没有重跑 HOLDOUT 排名。
- Draft PR：观察实现 head `a21afb20bd7bd7e6a4c777dc96ffd478e31fc3b0` 已推送到既有
  Draft PR #62；`ci-release` run `30752922314` 的 governance、web、api、containers 全绿，
  publish 因 Draft 跳过。没有转 Ready、合并或部署；回执提交后的最终 head 只确认 Checks 启动。

## 2026-08-02 公开数字口径卡本地候选

- 范围：只新增 `docs/product/metrics-card.md`、证据表入口、完整任务四文件与两层状态；不新增
  或重算数字，不修改产品代码、评测资产、Workflow、依赖、Provider 或生产。
- 覆盖：8 张卡统一语料 / 检索开发集、Stage 12、0/2→3/3 生成门、10 题 HOLDOUT、
  内存 / pgvector、公开运行 / 回放、预算 / 队列 / 输入 / 留存和 README 固定 CI 快照。
- 只读一致性：8 张卡结构完整，22 个仓库相对链接存在；四组 JSON、控制面常量、ADR、回放
  清单与 README 快照逐项匹配。正式评测、Provider、generation、产品运行调用均为 0。
- 边界：口径统一不构成新测量或新公开质量主张；开发集、未见集、单次真实调用、代码默认值
  与动态线上状态仍严格分开。待治理检查、提交推送和 Draft PR #62 最终 head Checks。
- 本地治理：114 tests 通过 / 8 skipped；公开仓 246 files / 8 public cases；园丁 stale 0 /
  review 1；差异检查通过且所有改动均位于 `docs/`。唯一 review 是既有迁移记录，不阻断候选。
- Draft PR：文档实现 head `b01adb9b160bc07f1f1c9f5d330e861f0ebdb07f` 已推送到既有
  Draft PR #62；`ci-release` run `30753847922` 的 governance、web、api、containers 全绿，
  publish 因 Draft 跳过。没有转 Ready、合并或部署；回执提交后再确认最终 head Checks 全绿。

## 2026-08-02 Stage 12 修复后首次复验预检

- 授权信封：用户夜班派发批准 DeepSeek `deepseek-v4-pro` 上用既有 runner 执行一次同一私有
  冻结集复验；24 案例 / 150 调用 / ¥10 上限，整套 1 次、自动重试 0、不补跑。
- 身份：绑定 `night-20260802@df01968c56350626544ca4acc4ed88cf13dfd337`；runner 与
  `origin/main` 为同一 blob；当前 prompt 集合 SHA-256 为 `108ab9...8584`，不同于原观测。
- 私有资产：24 题未见集仍在 Git 外，SHA-256 `7d730...8ab0` 与原观测精确一致，冻结检查通过。
- 预检：Stage 12 runner 13 tests 通过；隔离 worktree 缺模型缓存后，按清单核验主工作树 7 个
  BGE 文件并显式只读复用；旧响应离线回归完成 24 题。Provider 调用 0，真实复验待执行。
- 边界：本次是已消费集上的修复后回归观测，不恢复“全新未见”属性；原 `19/24、9 通过`
  记录不覆盖，不从前后差异推导单一修复的因果效果。

## 2026-08-03 Stage 12 修复后首次复验结果

- 唯一一次真实运行完整执行 24/24，2 题通过；39/150 次 Provider 调用、自动重试 0、
  未提前停止。runner 退出码 1 反映存在未通过题，不是装配失败；未补跑。
- 用量：私有回执有 35 条有效 usage，prompt 158,086、completion 139,320、total 297,406
  tokens；机制估算 ¥1.2599124 / ¥10。4 次调用没有有效 usage，故不写成账单确认。
- 逐维度：仅 insufficient-evidence 2/3，其余七维均 0/3；失败码为来源章节不匹配 15、
  必需事实缺失 12、outcome 不匹配 8、工单分类 / 优先级各 1。
- 身份：候选 `df01968`、镜像 `sha256:95d6...48a1`、模型 `deepseek-v4-pro`、prompt
  `108ab9...8584`、未见集 `7d730...8ab0`。公开聚合 `evals/stage12-post-fix-revalidation-v1.json`
  与 Git 外输出 SHA `2de8...57b9` 字节一致；私有原始 SHA `6eab...68af`。
- 边界：原始 19/24、9 通过记录不变；复验是已消费集回归，不是新未见评测，也不能从前后
  差异推导单一修复的因果效果。`product/0.1.0` 继续未发布，授权已消费且不得补跑。

## 2026-08-03 Stage 12 修复后复验本地候选

- 定向：Stage 12 15 tests 通过；公开 JSON 与 Git 外输出 SHA 字节一致，24 条私有输入、必需
  事实与凭据泄漏专项检查通过。
- 产品 / 治理：API 全集通过（4 个既有跳过，1 条 Windows 非阻断线程告警）；正式治理口径
  116 tests 通过 / 8 skipped；公开仓 251 files / 8 cases；园丁 stale 0 / review 1；差异检查通过。
- 身份未漂移：`df01968` 与当前工作树的 product / generation tree、Stage 12 runner blob
  一致；私有未见集 SHA 仍为 `7d730...8ab0`。待提交、推送与 Draft PR #62 最终 head Checks。

## 2026-08-03 Stage 12 修复后复验 Draft PR 实现回执

- 结果候选 `989f2eba23c0b074f77178bfae3e8f050f9c978c` 已推送到既有 Draft PR #62；
  `ci-release` run `30756898578` 的 governance、web、api、containers 全部成功。
- publish 因 Draft 跳过；PR 未转 Ready、未合并、未部署，Provider 授权没有扩展或再次消费。
- 状态回执提交会形成最终 head；只确认该 head required Checks，全绿后停止。

## 2026-08-03 Stage 12 失败根因归类本地候选

- 口径：公开聚合有 37 个失败码出现次数；派发的 36 个分组信号把 `SAF-003` 的 category /
  priority 两码合成了一个“其他案例”。37/37 个 `case_id + failure_code` 已唯一归入六类，
  重复 / 漏项 / 不存在项均为 0，分布为 6 / 12 / 6 / 3 / 4 / 6。
- 阶段定位：12 个 `required_fact_missing` 案例的冻结期望来源和事实均已进入 Top-10；直接根因
  分别落在生成合同、边界 / outcome 策略、handoff 评分、额外来源精确集合、义务规划和
  字面事实门。报告只保留公开案例 ID 与结构结论，私有明文继续留在 Git 外。
- 本地治理：显式只读复用经 7 文件大小 / SHA 校验的既有 BGE 缓存后，116 passed /
  8 skipped；公开扫描 255 files / 8 cases，园丁 stale 0 / review 1，465 条 Git 外长文本泄漏
  比对命中 0，差异检查通过。Provider 调用 0；不修代码、不跑评测、不改公开行为。
- 停止线：false-completion 的 safe candidate vs typed handoff 等产品取舍仅列候选，未替用户
  决定或实现；待提交推送并确认 Draft PR #62 最终 head required Checks。

## 2026-08-03 Stage 12 失败根因归类 Draft PR 实现回执

- 文档实现 head `8b5d1e6ef37ea4c1d0aba3da63471027c5b1933e` 已推送到既有 Draft
  PR #62；`ci-release` run `30757693288` 的 governance、web、api、containers 全部成功。
- publish 因 Draft 跳过；PR 未转 Ready、未合并、未部署，Provider 调用保持 0。
- 状态回执提交会形成最终 head；只确认该 head required Checks，全绿后停止。

## 2026-08-03 Stage 12 matched-handoff 评分合同本地候选

- 合同：expected / observed 同为 handoff 时只评分 outcome、明确登记 reason 与预算，实际
  boundary sources 只作 detail；其他 outcome 组合继续完整候选合同。未改生成、检索、知识、
  prompt、产品 package、公开 API 或用户可见 outcome。
- 离线重评分：固定 24 份既有 package，仅 `MBD-003`、`SAF-001/002/003` 四题移除预登记
  六码，新增码 0；其余 20 题逐题不变。失败码 37→31、通过 2→6 只表示 scorer-only 回归；
  历史聚合 SHA `2de8...57b9` 不变，新脱敏回执 SHA `d135...c88e`。
- 本地验证：Stage 12 19 tests；API 153 collected、149 passed / 4 skipped；治理 120 passed /
  8 skipped；公开扫描 260 files / 8 cases，园丁 stale 0 / review 1，465 条 Git 外长文本命中 0，
  差异检查通过。Provider / generation / Stage 12 调用均为 0。
- 边界：safe candidate vs typed handoff 未决定、未实现；待提交推送并确认 Draft PR #62 最终
  head required Checks，不转 Ready、不合并、不部署。

## 2026-08-03 Stage 12 matched-handoff 评分合同 Draft PR 实现回执

- 实现 head `eab5b20c527abe0e7aab57fbda577a4a15537d44` 已推送到既有 Draft
  PR #62；`ci-release` run `30759671469` 的 governance、web、api、containers 全部成功。
- publish 因 Draft 跳过；PR 未转 Ready、未合并、未部署，Provider 调用保持 0。
- 状态回执提交会形成最终 head；只确认该 head required Checks，全绿后停止。

## 2026-08-03 Stage 12 generation_shape 离线诊断候选

- 定位：目标案例 checklist 已过、第二次 Provider 调用成功，旧粗码发生在 claim / obligation
  binding 前的 QA content 外形层；历史响应正文未保留，精确子条件不可恢复且不猜测。
- 公开复现：四个合成 malformed 响应在基线 `cd1de058` 4/4 返回
  `top10_v6_content_invalid`；候选细分为 content 外形、identity、answer 外形和 claims 数量
  四码，合同级与产品链均定向通过，产品链仍 4/4 handoff。
- 边界：历史聚合 SHA `2de8...57b9` 与 handoff 重评分回执 SHA `d135...c88e` 不变；不运行
  Stage 12 / Provider，不放宽合同、不改 outcome，Provider 调用 0。
- 本地验证：定向 9 tests；API 162 collected、158 passed / 4 skipped；治理 120 passed /
  8 skipped；公开扫描 266 files / 8 cases，园丁 stale 0 / review 1，153 条 Git 外正文特征
  长字符串命中 0，差异检查通过。首次治理入口漏传本地模型根目录而在装配前失败，补齐只读
  路径后全绿；公开门捕获并修正活动目录显式指针。待提交推送与最终 head Checks。

## 2026-08-03 Stage 12 generation_shape Draft PR 实现回执

- 实现 head `c606bd316bfca1ac6f124f600c65c8c7128ac6ba` 已推送到既有 Draft PR #62；
  `ci-release` run `30760606556` 的 governance、web、api、containers 全部成功。
- publish 因 Draft 跳过；PR 未转 Ready、未合并、未部署，Provider 调用保持 0。
- 状态回执提交会形成最终 head；只确认该 head required Checks，全绿后停止。

## 2026-08-03 Stage 12 义务 / 来源机械合同候选

- 公开复现：规划遗漏基线为 `required_fact_missing`，候选改为
  `required_obligation_missing` 且仍失败；合法 extra-source 基线 exact-set 误扣，候选在型号、
  claim、obligation plan 三层账本完整时通过。正文遗漏、缺必需来源与无绑定 extra 继续失败。
- 24 题 scorer-only：通过数保持 6；R4 三个来源码删除，R5 四个事实码一对一换成义务码，
  `SO-001` 重叠，共 6 个案例变化、其余 18 题逐题不变，失败码出现次数 31→28。
- 边界：原聚合与 handoff / generation_shape 回执哈希不变；不运行 Stage 12 / Provider，
  不改 product / generation / prompt / outcome，Provider 调用 0。待全量治理、提交推送与最终
  head Checks。
- 本地验证：定向 3 tests、Stage 12 21 tests；API 162 collected、158 passed / 4 skipped；
  治理 122 passed / 8 skipped；公开扫描 272 files / 8 cases，园丁 stale 0 / review 1，153 条
  Git 外正文特征长字符串命中 0，差异检查通过。待提交推送与最终 head Checks。

## 2026-08-03 Stage 12 义务 / 来源 Draft PR 实现回执

- 实现 head `dd96538850017dac94d87e41940887bcecbc828d` 已推送到既有 Draft PR #62；
  `ci-release` run `30761545320` 的 governance、web、api、containers 全部成功。
- publish 因 Draft 跳过；PR 未转 Ready、未合并、未部署，Provider 调用保持 0。
- 状态回执提交会形成最终 head；只确认该 head required Checks，全绿后停止。

## 2026-08-03 Stage 12 typed handoff 边界本地候选

- 用户已选择 B 路线；六行生成前决策表编译为 `model_scope` / `evidence_gap` /
  `human_authority` typed handoff，公开 `GEN-DEV-MH-003` 产品差距关闭。
- 六个公开等价例先红后绿；已消费私有六例只读结构核验全部 handoff，transport factory /
  Provider 调用 0、无 candidate；未运行 Stage 12，不形成新模型质量结论。
- API 163 passed / 4 skipped、治理 122 passed / 8 skipped、Web 36 passed；公开扫描 276 files /
  8 cases、507 条新增私有长字符串命中 0；待提交推送和 Draft PR #62 新 head required Checks。

## 2026-08-03 Stage 12 typed handoff Draft PR 实现回执

- 实现 head `ace8a0e90044e6968d93cccd765ed2ad9b791580` 已推送到既有 Draft PR #62；
  `ci-release` run `30762836230` 的 governance、web、api、containers 全部成功。
- publish 因 Draft 跳过；PR 未转 Ready、未合并、未部署，Provider 调用保持 0。
- 状态回执提交会形成最终 head；只确认该 head required Checks，全绿后停止。

## 2026-08-03 Stage 12 今夜修复后唯一复跑

- 授权与身份：用户明文批准约 39 次 Provider 调用的唯一一轮、自动重试 0、不补跑；实际绑定
  `night-20260802@fc766709f48bf2989c6589a56db3cec7593ed6cb`、镜像 `sha256:c4a85b...b653`、
  `deepseek-v4-pro`、prompt `108ab9...8584`、私有集 `7d730...8ab0`。
- 预检：冻结检查 24 题通过，Stage 12 21 tests；旧响应离线装配 24/24、12 通过，六个 typed
  handoff 全通过；真实 Provider 调用 0。镜像、只读输入、Git 外输出和凭据存在性预检通过。
- 唯一真实运行：`03:33:37—03:58:44 +08:00` 同一容器 / 进程完整执行 24/24，11 题通过；
  28/150 次 Provider 调用、自动重试 0、未提前停止、`stop_code=null`，未补跑。
- usage / 费用：27 条有效 usage，prompt 120,799、completion 94,377、total 215,176 tokens，
  cache hit 71,168 / miss 49,631；1 次调用无有效 usage。机制估算 ¥0.7169342 / ¥10，非账单确认。
- 失败与 typed handoff：失败码 14 次（事实 6、义务 6、outcome 1、source 1）；六个预登记
  typed handoff 全部 type + reason 命中、0 调用并通过；连同既有边界共 10 个零调用案例。
- 对比：首次真实复验 2 通过 / 37 码 / 39 调用 → 本轮 11 / 14 / 28；31 码与 28 码是同一旧
  package 的两份离线 scorer 口径，不与 2 通过拼成一个观测。差异只作描述，不作因果结论。
- 证据：公开聚合 SHA-256 `b4de5028...67e8` 与 Git 外输出字节一致；脱敏回执 SHA-256
  `8c80b771...80fd`；Git 外原始记录 SHA-256 `73d272e9...c850`。私有正文与凭据未进入 Git。
- 本地交付门：Stage 12 22 passed；API 163 passed / 4 skipped；治理 123 passed / 8 skipped；
  Web lint / typecheck / build 与 36 tests；公开扫描 282 files / 8 cases；园丁 stale 0 / review 1；
  738 条相对 HEAD 新敏感长字符串命中 0；差异检查通过。
- 当前检查点：本地候选已绿，待提交推送和 Draft PR #62 最终 head Checks。Provider 授权已
  耗尽，不允许第二轮；不转 Ready、不合并、不部署。

## 2026-08-03 Stage 12 今夜复跑 Draft PR 实现回执

- 结果实现 head `adf5c74a53756019f6ceae7b745468b2fd703802` 已推送到既有 Draft PR #62；
  `ci-release` run `30765242124` 的 governance、web、api、containers 四项 required jobs
  全部成功，publish 因 Draft 跳过。
- PR 保持 Open / Draft / CLEAN，未转 Ready、未合并、未部署；Provider 授权已由唯一一次
  28 调用复跑消费完毕，不允许补跑。
- 状态回执提交会形成最终 head；只确认该 head required Checks，全绿后停止。
