# 计划

1. 锁定今夜聚合、已消费私有集和原始 package 的 SHA-256，机械提取六个 R6 案例。
2. 给每条冻结事实分配案例内 `P<n>`，逐条核对 approved clause、obligation、claim 和客户可见表达。
3. 生成不含私有明文的 ID / 哈希审计收据，分开统计真遗漏和字面假阴性。
4. 形成只替换 R6 visible substring 的评分方案候选，明确缺绑定继续失败与语义真实性边界。
5. 更新结果与两层状态，运行定向、API、治理、公开安全和泄漏检查。
6. 提交推送 `night-20260802`，确认 Draft PR #62 最终 head required Checks 后归档派发。

停止线：固定身份不一致、六题集合漂移、任一结论缺少命题 / 义务 / claim 证据、需要无约束
LLM judge、需要修改 scorer / generation，或需要新 Provider 输出时立即停止。
