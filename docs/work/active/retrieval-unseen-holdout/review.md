# 评审与边界

## 方案判断

- 现有 16 题已经引用生产知识的全部 27 个章节；继续复用这些章节无法满足“知识内容不得与
  开发集重复”，因此采用不进入产品语料的独立公开合成评测知识单元。
- 一次性工具在输出存在时拒绝覆盖，并要求完整 freeze commit 中的题集字节与当前文件一致；
  这样可以机器证明排名发生在冻结之后。
- 标准 CI 只校验冻结结构和落盘回执，不重新运行“首次观测”；揭示后的资产只允许回归用途。

## 当前阻断与授权

当前没有方案级阻断。用户授权推送 `night-20260802` 并更新 Draft PR；不授权 Ready、合并、
部署、Provider、生成、费用或生产语料变更。
