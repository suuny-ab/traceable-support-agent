# 计划

1. 锁定公开聚合、候选、私有集与原始记录哈希；确认本任务 Provider 调用为 0。
2. 把每个失败案例拆成检索、义务规划、生成、边界判定、评分五段结构化信号。
3. 对 37 个 `case_id + failure_code` 建立唯一根因映射，并用集合检查证明无漏项 / 重复项。
4. 每类选取不泄露私有明文的代表案例，记录能证明、不能证明与最小修复候选。
5. 更新结果报告、数字口径卡与两层状态；运行治理、泄漏和差异检查。
6. 提交并推送 `night-20260802`，确认 Draft PR #62 最终 head required Checks 后停止。

停止线：任何私有明文进入 Git、需要修改产品 / 评分器、需要 Provider 或需要决定公开行为时，
立即停在报告和候选方案，不在本任务继续实现。
