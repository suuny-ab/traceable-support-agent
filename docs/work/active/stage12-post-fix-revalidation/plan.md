# 执行计划

1. 核对派发授权、当前 Draft PR、runner blob、私有未见集 SHA、模型与 prompt 集合身份。
2. 在 Provider 调用前运行冻结检查、Stage 12 定向测试与旧响应离线回归；只修环境装配问题。
3. 从冻结候选构建 `live` target 执行镜像，记录镜像摘要并确认 runner / 私有输入只读挂载。
4. 按验证卡只执行一次真实复验；无论成功、失败或提前停止都不补跑。
5. 将原始记录留在 Git 外，把脱敏聚合复制为新的公开报告，并机械核对数字、失败分类与用量。
6. 更新数字口径卡、证据表、PROJECT 与两层状态；保留原观测，不扩大结论。
7. 复跑相关测试与治理，提交并推送 `night-20260802`，确认 Draft PR #62 最终 head Checks 全绿。
8. 归档派发，在战报顶部追加带时间戳的三行后停止；不转 Ready、不合并、不部署。
