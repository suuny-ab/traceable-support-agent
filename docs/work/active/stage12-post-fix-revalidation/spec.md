# Stage 12 修复后首次复验

## Goal

在 `night-20260802` 上用既有 Stage 12 runner 对原 24 题私有未见集执行一次修复后复验，
把新结果与 `2026-07-24` 的原始 `19/24、9 通过`观测并列公开，回答已知生成门与
SAF-003 / MBD-003 边界修复后，同一冻结集上的机械合同观测变成了什么。

## Non-goals

- 不修改 runner、评分器、未见集、离线响应、prompt、生成门、检索、知识或公开回归案例。
- 不针对原结果或本次结果调案例、抬分、补跑失败题；全量只执行一次，自动重试 0。
- 不覆盖 `evals/stage12-aggregate-v1.json` 或倒写原始完成记录。
- 不把复验写成开放域准确率、线上成功率、Stage 12 “通过”或 `product/0.1.0` 发布结论。
- 不转 Ready、不合并 `main`、不部署、不修改生产配置或凭据。

## AC

1. **WHEN** 执行前检查私有资产，**THEN** 未见集仍为 24 题且 SHA-256 精确等于
   `7d73073cd0227b0ced81398fcbadc7e5f85867a633a9654d82bd0b516c358ab0`，冻结检查通过。
2. **WHEN** 锁定复验身份，**THEN** 报告绑定候选
   `df01968c56350626544ca4acc4ed88cf13dfd337`、模型 `deepseek-v4-pro`、prompt 集合
   `108ab9aae60eb86806383cc2fea4511d358955f50503531e0da2e82be1ba8584` 与执行镜像摘要。
3. **WHEN** 发起真实复验，**THEN** 仅调用既有 `tools/stage12_eval.py`，上限为 24 案例、
   150 次 Provider 调用、¥10 机制估算费用，自动重试 0，整套重复 1 次。
4. **WHEN** runner 命中费用 / 调用信封、执行异常或 Provider 执行失败停止条件，**THEN**
   立即停止并保留该次结果，不补跑、不改代码或案例后重试。
5. **WHEN** 复验结束，**THEN** 新公开聚合报告只含案例 ID、逐题机械结果、失败分类、
   身份和用量，不含私有输入、期望事实、Provider 原文、请求头或凭据；原始记录仅留 Git 外。
6. **WHEN** 核对结果，**THEN** 完成数、通过数、逐维度数、失败分类、Provider 调用数、
   token / 费用字段和停止原因均从新聚合与私有原始记录机械汇总一致。
7. **WHEN** 更新公开口径，**THEN** 原 `19/24、9 通过`记录保持不变，新结果以
   “2026-08-02 修复后首次复验”并列，明确原集已消费、今后只可回归。
8. **WHEN** 完成交付检查，**THEN** Stage 12 定向测试、相关 API / 治理、公开仓扫描、
   文档园丁和差异检查全部通过；私有明文与凭据未进入 Git。
9. **WHEN** 提交候选，**THEN** 仅推送 `night-20260802` 并更新 Draft PR #62，确认最终 head
   required Checks 全绿；不转 Ready、不合并、不部署。

## 验证说明卡

- **问题**：同一冻结私有集在已知生成门 / 边界修复后的当前产品候选上，机械合同观测为何？
- **候选身份**：`night-20260802@df01968c56350626544ca4acc4ed88cf13dfd337`；后续只允许
  增加本任务回执，必须证明 runner、生成 / 检索 / 知识与 prompt 哈希没有漂移。
- **Provider / model**：DeepSeek / `deepseek-v4-pro`，目的仅为 Stage 12 修复后复验。
- **固定输入**：Git 外 24 题原私有集；SHA-256
  `7d73073cd0227b0ced81398fcbadc7e5f85867a633a9654d82bd0b516c358ab0`。
- **评分**：复用既有 runner 的逐题 outcome、来源章节、必需事实、工单分类 / 优先级与预算
  机械评分；不新增阈值。
- **信封**：最多 24 案例、150 调用、¥10 机制估算费用；自动重试 0；整套重复 1 次；
  不补跑。单案例仍由 runner 限制最多 2 次调用。
- **硬停止**：费用 / 调用信封不足、执行异常、真实模式生成包含
  `execution_failure:`、凭据或私有资产缺失、身份 / 哈希漂移、输出落入 Git 时立即停止。
- **授权**：用户在 `2026-08-02` 夜班派发中明文批准本次既定 runner 的一次复验；不扩展
  到第二轮、其他 Provider / model、生产写入、Ready、合并、部署或发布判断。
- **最便宜证伪**：Provider 前先做冻结检查、runner 定向测试和旧响应离线回归；离线结果只
  证明装配可运行，不冒充本次真实结果。
- **允许结论**：该候选、模型、prompt 与同一已消费冻结集上的一次描述性机械观测。
- **禁止结论**：开放域 / 客户质量、稳定成功率、因果改善、生产 SLA、发布成熟度。

## 回滚

候选阶段关闭 Draft PR #62 并删除 `night-20260802`；若未来合并则 revert 对应 squash。
公开聚合与文档回执可删除，Git 外原始记录保留用于审计；原始 Stage 12 观测不受影响。

## 规则复述

- Provider 调用只能使用本卡固定的 provider、model、目的、上限、重试 0 和停止条件；不补跑。
- 私有未见集、原始 Provider 内容、凭据和环境清单永不进入 Git；公开报告只留聚合。
- 本任务只到集成分支与 Draft PR；不转 Ready、不合并、不部署、不发布 `product/0.1.0`。
