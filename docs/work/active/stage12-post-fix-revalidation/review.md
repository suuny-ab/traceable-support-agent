# 评审与边界

## 执行前判断

- 本任务为完整 / R2：读取 Git 外私有未见集并产生真实 Provider 调用与有界费用。
- 用户在本轮要求执行派发；派发明确批准既有 runner 的一次修复后复验、自动重试 0、不补跑，
  因而授权只覆盖验证卡中的 DeepSeek / `deepseek-v4-pro`、24 案例、150 调用、¥10 与一次执行。
- runner、评分器和私有集均不改；当前 prompt 集合与原观测不同，必须作为新身份公开，不能沿用
  原 `dccfa...` 哈希或把结果解释成只由某一项修复导致。
- 原集已消费，本次属于修复后回归观测，不恢复“全新未见”属性；结果只可描述当前候选。

## 当前阻断

无。真实执行前仍须完成镜像构建与摘要绑定；任何身份、凭据、挂载或信封异常都停止且不调用。
