# 评审与边界

## 执行前判断

- 本任务为完整 / R2：读取 Git 外私有未见集并产生真实 Provider 调用与有界费用。
- 用户在本轮要求执行派发；派发明确批准既有 runner 的一次修复后复验、自动重试 0、不补跑，
  因而授权只覆盖验证卡中的 DeepSeek / `deepseek-v4-pro`、24 案例、150 调用、¥10 与一次执行。
- runner、评分器和私有集均不改；当前 prompt 集合与原观测不同，必须作为新身份公开，不能沿用
  原 `dccfa...` 哈希或把结果解释成只由某一项修复导致。
- 原集已消费，本次属于修复后回归观测，不恢复“全新未见”属性；结果只可描述当前候选。

## 结果判断

- 执行身份、未见集 SHA、runner、模型、prompt、镜像、信封和重试数均与验证卡一致；唯一
  一次运行完整执行 24 题，没有提前停止或补跑。
- 公开聚合与 Git 外 runner 输出字节一致；公开面只含案例 ID、机械结果、失败分类、身份和
  信封，不含私有输入、期望事实、Provider 原文、请求头或凭据。
- 24/24、2 通过显著弱于可形成质量主张所需的证据，且安全 / 型号维度均为 0/3 全合同通过。
  这会继续阻断 Stage 12 “通过”、开放域质量改善与 `product/0.1.0` 发布主张，但不阻断
  本次诚实回执进入 Draft 候选。
- 不安排独立 Reviewer：数字、哈希、逐维度统计、失败分类和用量均可由机器结果直接验证，
  没有未决方案级疑问。

## 授权收口

本次 Provider / 费用授权已由唯一一次执行消费完毕；不允许第二轮、失败题补跑、调案例、
换 model、扩大费用或把 PR 转 Ready / 合并 / 部署。后续只做本地检查、状态、提交、推送和
Draft PR Checks。
