# 计划

1. 锁定历史聚合、R6 语义审计、Git 外原始 package 与 scorer 身份。
2. 添加公开合成正反例，先证明旧字面判据误扣改写且漏掉缺 claim / 越源 / 伪造 ID。
3. 实现 proposition → obligation → claim / evidence 的 host 侧确定性收据验证。
4. 对固定 24 个已消费 package 只读重评分，确认六个 R6 案例与其余 18 例的变化边界。
5. 固化脱敏收据、冻结测试和两层状态，运行全套测试与治理检查。
6. 提交推送 `night-20260802`，确认 Draft PR #62 最终 head required Checks 后归档派发。

停止线：固定身份不一致、公开反例失去失败关闭、除六个 R6 外发生分类漂移、需要语义 judge、需要
新 Provider 输出，或需要 Ready / merge / deploy 时立即停止。
