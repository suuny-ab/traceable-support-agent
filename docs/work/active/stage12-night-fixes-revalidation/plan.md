# 执行计划

1. 冻结候选、runner、prompt、私有集、模型、信封和旧对照口径。
2. Provider 前执行冻结检查、Stage 12 定向测试和旧响应离线装配回归。
3. 从 `fc766709` 构建 live 候选镜像，记录摘要并验证只读输入 / 可写 Git 外输出。
4. 执行唯一一次真实复跑；只监控同一容器，不补跑。
5. 机械生成脱敏聚合、逐维度 / 逐失败码 / typed handoff / usage 对比。
6. 更新公开口径、证据表、稳定事实与两层状态，保留历史资产不变。
7. 运行相关测试与治理，提交推送 `night-20260802` 并确认 Draft PR #62 最终 Checks。
8. 等哈希归档派单，在战报顶部追加带时间戳的三行后停止。
