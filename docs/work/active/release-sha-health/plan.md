# 实现计划

1. 复用 `VCS_REF`，把合法的 40 位 Git SHA 烘焙为 API 镜像只读文件，并由健康接口返回。
2. 让 CI 回放镜像、生产 live 构建、候选激活、公开冒烟和适用的回滚目标核对 manifest SHA。
3. 补 API、发布和仓库扫描测试，运行镜像冒烟与全量本地回归，更新当前事实并形成单一提交。

不实现监控、Web 展示、RAG / Provider / pgvector 变化或外部部署。
