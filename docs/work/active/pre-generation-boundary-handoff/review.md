# 独立复核

> 状态：`findings_open`

本增量触及安全边界与公共失败关闭合同，正式独立复核必须在 Draft PR 的
`governance`、`web`、`api`、`containers` 四项 Checks 全绿、head SHA 冻结且主 Agent
停止写入后执行一次。复核者只读，不修改候选。

## 首轮回执

- 候选 SHA：`8b50618de0f59b623de8e7314201931d2310c6a8`。
- 基线 SHA：`0bb97c974d2a5cdc7d455f329ee81aca9a9f9755`。
- Draft PR：`#24`。
- 前置证据：CI run `30058941904` 的 `governance`、`web`、`api`、`containers`
  四项 Checks 全绿；`publish` 因 Draft 按设计跳过。
- 复核方式：固定候选、主 Agent 停止写入、同一工作树只读独立复核。
- 结论：`failed`；该 SHA 不得合入。

### 阻断发现

1. 输入正文只明确提到一个型号、但与 `product_model` 不一致时，型号边界可能被绕过。
2. `product-boundary-handoff-v1` 将来源放在 `boundary_sources`，Stage 12 正式评分器仍只从
   生成结果的 `used_evidence_ids` 取来源，合法 handoff 会被误判为来源不匹配。
3. CZ-R1 的型号排他词规则同时拦截了“有没有自动集尘”和 R1 / R2 差异等合法知识问答。
4. `起火`、`触电` 不在当前规则声明的公开合成来源内，不能作为有来源的安全升级词。

四项均可由公开 API / 正式评分入口触达，且会改变用户行为或结论真实性，因此满足阻断
标准。后续只修复这些 finding 及其覆盖；新候选四项 Checks 全绿后，由同一复核者仅复核
finding 与覆盖 diff。

## 第二轮聚焦回执

- 候选 SHA：`46c753773795d8ae97a8239bbb9fc1f185cc3c09`。
- 前置证据：CI run `30059598825` 的四项必需 Checks 全绿；工作树干净且候选固定。
- 复核范围：首轮四项 finding 与覆盖 diff；同一复核者只读执行。
- 已关闭：单一明确型号冲突、Stage 12 `boundary_sources` 评分、无生成包回归，以及删除
  无来源的 `起火` / `触电` 安全词。
- 结论：`failed`；该 SHA 不得合入。

### 剩余阻断发现

1. 同时提到 CZ-R1 / CZ-R2 的文本跳过单一型号冲突检查；若所选型号为 CZ-R2，包含
   CZ-R1、R2 独占能力和操作标记的请求仍可绕过边界。
2. 能力存在问答未覆盖“可以……吗”“可不可以”“会不会”，合法知识问答仍会被误转人工。

后续只增加这两类表达及相邻操作负例；新候选四项 Checks 全绿后，仍由同一复核者仅检查
剩余 finding 与覆盖 diff。

## 第三轮聚焦回执

- 候选 SHA：`497f10cb2236b4760ed13368373d889522dbc661`。
- 前置证据：CI run `30059878623` 的四项必需 Checks 全绿；工作树干净且候选固定。
- 已关闭：双型号操作 / 故障请求绕过，以及“可以 / 可不可以 / 会不会”已有问法。
- 结论：`failed`；该 SHA 不得合入。
- 剩余阻断：`开启`、`启动`、`设置` 等歧义动作词先于能力模式判定，使“是否支持开启
  自动集尘功能”和“自动集尘可以设置吗”仍被误拦。

后续仅调整这项判定优先级并补充复核者给出的两个正例；“怎么 / 怎样 / 如何”、明确故障
状态及“可以怎么更换”仍作为操作请求阻断。新候选继续走四项 Checks 和同一复核者的
finding-only 复核。
