# 执行计划

> 状态：`completed`

1. 建立 `codex/meta-development-governance-audit` 分支和本活动工作；把 #7 标记为唯一活动
   入口，并在 #5、#6 留下由 #7 统一裁决的回执。
2. 抽样 PR #4、#8/#9、#10/#11，记录周期、CI 尝试、复核次数、候选失效、有效 findings、
   重复文档、平台噪声和逃逸缺陷；同步修正文档漂移。
3. 落地最小复核门、GitHub 生命周期、Issue 表单 / 标签和路线 Issue。
4. 增加失败关闭的 CI 影响分类、四个稳定检查、`release-decision` 和无 production
   environment 的部署预检；保留旧发布 run ID 的手动恢复兼容。
5. 完成四页和共享导航的商业 SaaS 重设计，不改变运行合同。
6. 运行最便宜且相关的本地检查，补齐 Candidate / Product / 浏览器验证。
7. 提交并推送 Draft PR，等待四个 Checks 全绿；冻结 head SHA、停止写入并调用一个正式
   只读复核者。
8. 若有阻断，提交修复、重跑 CI，并只针对 finding 与覆盖 diff 复核；否则不增加复核轮次。
9. 合并后验证自动部署、服务器正式回执、容器、公网五路由和 Provider 边界。
10. 用户验收后创建治理类收口 PR；证明它不发布、不进入生产、不执行 SSH，再移动活动记录
    并关闭 #5、#6、#7。

全部步骤已按顺序完成。实现 PR 为 #16；用户于 `2026-07-23` 完成线上体验验收。本记录
所在收口 PR 只包含治理白名单路径，作为检查点 G 的最终证据。

## 检查点

- A：活动工作与 GitHub 所有权一致。
- B：审计证据与治理候选完成。
- C：CI 分类 / release decision 失败关闭。
- D：四页产品与浏览器验证通过。
- E：绿色 Draft PR 固定 SHA。
- F：正式复核、自动部署和生产回执通过。
- G：用户验收与治理类收口通过。
