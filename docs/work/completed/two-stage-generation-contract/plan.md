# 实施计划

> 状态：`closed_not_planned`

1. 固定 Issue #22 的公开证据、失败分类、范围、最便宜证伪和本地 R0 验证说明卡。
2. 为 QA / 工单结果增加稳定的失败阶段与原因族统计，不改变现有 handoff 原因码。
3. 用公开合成夹具复现“语义正确但机械重复不完整”和“真实来源 / 事实错误”两类结果。
4. 设计最小合同候选：语义选择仍由外部 LLM 完成；义务 ID、已审定证据绑定和可推导
   投影由宿主生成；客户可见事实仍逐字绑定来源。
5. 单独审查 Provider 响应信封合同；只有正式文档或有界探针证明的可选差异才兼容。
6. 先运行最便宜单元测试，再运行 Fast、Candidate 和 Product 相关检查。
7. 冻结外部 API 验证说明卡，固定 Provider / model / prompt / schema / 数据 / 调用上限 /
   最坏费用 / 重试 0 / 停止条件；未授权时停止。
8. 授权后直接调用外部 API 做兼容性探针和固定合成对照；不使用本地小模型。
9. 更新结果和产品事实；推送、Draft PR、正式复核、合并与部署分别遵守外部门。

## 第一次 R1 后的续行

第一次真实探针执行 3/4 例、4 次调用后因 Provider 信封失败硬停止，旧授权已关闭。后续
只允许：

1. 在本地用 NFKC 统一检索文本与公开事实的评分边界；不得把标点兼容误写成 LLM 质量
   提升。
2. 把 checklist 与 Provider 信封错误细分为只反映字段 / 子合同的稳定原因码，不保存
   Provider 原始正文。
3. 公开记录 transport 状态、延迟、成功计价调用和未获得 usage 的调用数。
4. 用既有私有 candidate 做离线重评分，不重复付费运行已经能证伪的 QA 案例。
5. 新诊断只固定 `GEN-DEV-QA-003` 与 `GEN-DEV-TK-001`，最多 4 次调用、最坏
   `¥1.40`、重试 0；建立新 attempt 并重新授权。
6. 得到具体子码后再决定接受正式文档允许的信封差异、调整 checklist，或停止本方向。

## 第二次 R1 后的续行

第二次诊断执行 2/2 例、3 次调用后，QA-003 形成正确 candidate，TK-001 因非 `stop`
`finish_reason` 硬停止。后续只允许：

1. 把公开探针的来源要求解释为“所有必需来源必须出现”；额外来源必须已经通过产品的
   逐字 claim / 义务 / 来源绑定硬门，探针不替代语义相关性判断。
2. 按 DeepSeek 官方枚举把非 `stop` 原因拆成隐私安全子码，但继续全部失败关闭。
3. 修正公开报告的 `provider_response_received` 字段投影并公开实际使用来源章节。
4. 用既有 QA-003 私有合成包离线重评分；不再为该例付费。
5. 下一次只固定 `GEN-DEV-TK-001`，最多 2 次调用、最坏 `¥0.70`、重试 0；不改变
   prompt、thinking 或 token 上限，先确认实际停止原因。
6. 若得到 `length`，再单独设计输出预算 / prompt 收敛候选；若为过滤、工具调用或资源
   中断，按对应官方语义处理。不得把非 `stop` 截断正文兼容成成功。

## 第三次 R1 后的续行

第三次诊断只调用 1 次，确认 TK-001 第一阶段为 `finish_reason=length` 并硬停止。后续：

1. 保持模型、prompt、thinking、response schema、来源和安全门不变，只把第一阶段
   `max_tokens` 从 `8192` 提高到 `16384`。
2. 预算必须从实际请求体读取阶段 `max_tokens`；QA 与工单共用唯一阶段配置，防止请求
   和预留漂移。
3. 报告显式记录两阶段 token 上限和实际 `timeout_ms=180000`；更正早期卡片把未使用的
   `30s` 常量误写为运行时超时的问题。
4. 新验证只固定 TK-001，最多 2 次调用、最坏 `¥0.70`、重试 0；建立
   `issue22-public-synthetic-length-recovery-4` 并重新授权。
5. 若第一阶段再次 `length`，停止并评估更高额度或 prompt 收敛；若进入第二阶段，则
   收集完整候选或下一项稳定合同失败，不扩大到其他案例。

## 第四次 R1 后的续行

第四次验证只调用 1 次，`length` 已消失，第一阶段转为候选质量失败
`two_step_checklist_obligation_count_invalid`。后续：

1. 不放宽义务上限，先把聚合码拆成类型、空列表和超过 8 项三个安全子码。
2. 模型、prompt、thinking、token、超时、预算和公开案例保持 v4 不变。
3. 在当前仓外常设授权下，固定 v5 单例、最多 2 次、最坏 `¥0.70`、重试 0；执行前仍
   必须冻结代码 / 镜像 / 报告身份。
4. 若为超过 8 项，审查语义义务是否被过度拆分，再设计 prompt 或宿主合并边界；若为空
   或类型错误，先修输出合同表达。不得仅为通过而扩大上限。

## 第五次执行后的续行

第五次验证在与 v4 相同的 prompt 和请求配置下完成 TK-001 两阶段并通过全部合同，没有
复现义务数量失败。后续：

1. 不因单次成功删除数量子码，也不把 v4 误写成已修复的确定性缺陷；当前证据只说明该
   失败不是稳定复现。
2. 不重复付费运行已经得到合法候选的 QA-003、QA-006 或 TK-001。
3. 原始四例中只对从未执行的 TK-006 建立 `remaining-ticket-v6` 单例；产品行为、
   prompt、请求配置和全部硬门保持不变。
4. v6 最多 2 次调用、最坏 `¥0.70`、重试 0；成功则进入增量收敛，合同或执行失败则
   按现有分类停止或形成下一项有界事实。

## 第六次执行后的续行

第六次验证的两次 Provider 调用都正常完成，checklist 与 ticket 合同均通过，最后仅因
`completeness_gate_failed` 转人工。禁止读取仓外原始正文调优。后续：

1. 不把关键片段机械补进客户回复，也不因单次失败放松 completeness gate。
2. 同一代码、镜像、prompt、案例和请求配置建立新的独立 attempt，只复现一次 TK-006。
3. v7 最多 2 次调用、最坏 `¥0.70`、重试 0；若再次是 completeness，则进入结构性
   合同设计；若通过，则记录模型波动，不宣称稳定成功。
4. 任一执行完整性失败仍立即硬停止；不得恢复 v6 或复用其剩余额度。

## 第七次执行后的续行

第七次使用与 v6 完全相同的候选和案例通过全部合同，证明 TK-006 的逐字 completeness
结果存在通过 / 失败波动。后续停止对该版本重复抽样：

1. 在本地建立客户可见语义跨度合同：LLM 对每条来源 claim 同时给出客户文本中表达该
   主张的连续片段，并把 claim 归属已审定义务。
2. 宿主只验证客户片段确实存在、来源片段逐字存在、义务 / 来源绑定完整且每项义务都有
   客户可见 claim；不再用规则判断客户改写是否逐字包含 checklist 的 key_elements。
3. 安全、型号、隐私、预算、Provider 信封、来源逐字真实性和人工批准边界保持确定性
   失败关闭；LLM 的语义映射不能越过这些硬门。
4. 先用“语义等价改写”“伪造客户片段”“错误来源”“漏义务”四类公开合成夹具证伪；
   R0 通过后才冻结新的外部 API 候选。

## v8 本地候选后的续行

v8 已在 QA 与工单采用 `customer_visible_span_text`，并通过公开合成与产品测试。后续：

1. 先只验证直接暴露波动的 TK-006；不同时扩大到 QA 或其他案例。
2. v9 固定当前代码、镜像、新 ticket schema / prompt、TK-006、最多 2 次调用、
   `¥0.70`、重试 0。
3. 若模型遵守新 schema，宿主验证来源跨度、客户跨度、义务与来源后形成 candidate；
   任一伪造跨度、错误来源、漏义务或执行完整性问题继续失败关闭。
4. v9 之后再决定是否需要独立 QA 单例；不得把单个 ticket 成功写成 QA 或开放域结论。

## v9 外部通过后的续行

v9 证明真实模型在 TK-006 上遵守 ticket v3 语义跨度合同并形成 candidate。后续：

1. QA 使用独立的 `retrieved-top10-qa-result-v4` 和 QA prompt，必须单独验证。
2. 新增只选择既有代表案例 QA-003 的 `semantic-qa-v10` profile，不改变产品行为。
3. v10 固定当前代码、镜像、prompt、QA-003、最多 2 次调用、`¥0.70`、重试 0。
4. v10 完成后停止扩大外部样本；结果无论成功或分类失败，都进入增量收敛与 Draft PR 门。

## v10 外部结果后的收敛

v10 第一阶段以 `two_step_checklist_key_elements_invalid` 转人工，没有触达 QA v4。后续：

1. 不重跑 v10，不读取仓外正文，不把第一阶段失败误写成 QA v4 不兼容。
2. 保留 ticket v3 的真实通过证据、QA v4 的 R0 证据和真实兼容性未知。
3. 运行最终 API、工具、公开扫描与 diff 检查；更新当前事实和路线，不改写 Stage 12。
4. 停在推送 / Draft PR 独立外部动作前；获得授权后推送并创建 Draft PR，等待四项
   Checks 全绿再冻结 head SHA 和进行一次正式只读复核。

## v10 后的 v4 合同修正

用户确认继续同一 Issue #22 增量，不另建 Issue，并明确不要本地小生成模型。后续：

1. 第一阶段 checklist v4 只让 LLM 输出义务描述与语义支撑的 clause IDs；宿主验证
   clause 身份、完整互斥分区，并机械派生 evidence 和忽略上下文。
2. 删除生产路径的 `key_elements` 复制、验证和第二阶段下传；保留历史 v2 兼容路径。
3. 先通过 Fast、Candidate、Web Product、公开扫描和无网络 live 镜像检查。
4. 冻结同一代码 / 镜像的 QA003 与 TK006 两个独立单例 attempt，各最多 2 次调用、
   `¥0.70`、自动重试 0；QA 的候选质量失败不冒充执行完整性失败。
5. 若 QA attempt 出现授权、transport、预算、身份或包完整性失败，则不启动 ticket；
   否则按独立卡执行 ticket。两次都不读取 Provider 原始正文调优。
6. 更新同一 Draft PR，等待最终四项 Checks 全绿后冻结 head，再做一次正式只读复核；
   合并和部署仍是后续独立动作。

## v11 硬停止后的长度恢复

v11 的 checklist v4 第一阶段通过，QA 第二阶段以
`provider_response_finish_reason_length` 执行失败关闭；按卡片未启动 v12。后续：

1. 不恢复 v11、不复用其额度、不读取原始正文；把 v12 保持为未执行事实。
2. 根据脱敏回执和 Provider 官方合同，把第二阶段最大输出从 `8192` 增至 `16384`；
   schema、prompt、thinking、超时、重试和全部硬门保持不变。
3. 重新运行完整 R0、预算公式和无网络镜像检查。
4. 建立新 `qa-length-recovery-v13` 单例，仅执行 QA003，最多 2 次调用、`¥0.70`、
   自动重试 0；任一执行完整性失败立即关闭。
5. v13 形成安全回执后，才决定是否需要用新候选恢复独立 ticket 验证；不得自动执行
   旧 v12。

## v13 数量波动后的最后一次对齐

v13 第一阶段以 `two_step_checklist_obligation_count_exceeded` 转人工，第二阶段 16K 未
触达。公开输入有 10 条证据、70 个 clause；宿主限制最多 8 项，但 v13 prompt 未声明
这个限制。后续：

1. 不读取原始正文，不提高 8 项或 3KB 上限。
2. 在第一阶段 prompt 明示 1–8 项，允许把紧密相关分支在同一义务中完整表达，禁止按
   证据句子机械拆分；独立分支仍不得遗漏。
3. 用测试锁定 prompt 与宿主共享同一 8 项边界，完整运行 R0 与无网络镜像检查。
4. 建立 `checklist-count-alignment-v14`，仍只执行 QA003、最多 2 次调用、`¥0.70`、
   重试 0；这是本轮最后一次付费调参验证。
5. 无论 v14 成功或形成安全分类失败，都停止扩大付费样本，转入事实收敛与 Draft PR。

## v14 后的收敛

v14 两阶段合同通过并形成 candidate，但公开评分仍为 `required_fact_missing`。后续：

1. 不读取私有正文、不继续付费调参，不把 candidate 等同于完整事实通过。
2. 保留 Issue #25 的语义评估边界，不在 Issue #22 内把逐字评分临时改成 LLM 裁判。
3. 更新稳定事实、活动结果、失败统计和路线；Issue #22 保持开放。
4. 运行最终 Fast / Candidate / Product / 公开扫描与 diff 检查，推送同一 Draft PR #27。
5. 等待最终 `governance`、`web`、`api`、`containers` 全绿，冻结 head SHA 后主 Agent
   停止写入并进行一次正式只读复核。
6. 复核通过也不自动合并、部署或关闭 Issue；这些仍需要后续明确决定。

## 首次正式复核 finding 后的修正

冻结 head `83511d4b71c649689295a7bd88720b1bf4fd4b78` 四项 Checks 全绿后的正式只读
复核结论为 `changes_required`：

1. 第一阶段按 clause 选择 / 忽略，但第二阶段只按 evidence 校验，允许同一 evidence
   中被忽略的 clause 冒充所选义务来源；该问题改变结论真实性，属于 P1。
2. 宿主为每项义务机械派生 `approved_source_spans` 并下传第二阶段；QA / 工单 claim
   的 `exact_span_text` 必须位于其绑定每项义务、每个 evidence 的允许 clause 范围。
3. 新增 QA / 工单同 evidence 选中与忽略 clause 交叉引用攻击回归；失败码分别为
   `top10_v8_clause_binding_invalid` / `ticket_v4_clause_binding_invalid`。
4. 运行完整 R0、公开扫描和无网络镜像检查，不调用 Provider；v14 外部回执不迁移为
   v15 真实兼容证据。
5. 推送新 head、等待四项 Checks 全绿后，只针对 finding 与覆盖 diff 复核；旧正式
   回执随候选变化失效。
