# 已知限制

- 仅使用合成知识和合成工单。
- 公开服务仍为单机 Beta；当前真实 Provider live 已启用，但不承诺生产级可用性，实时不可用时只保留明确标记的已验证回放。
- Stage 12 原始正式观测为 19/24 案例、9 通过，且当时 SAF-003 / MBD-003 未正确转人工。
  Issue #21 增加公开合成确定性回归后，`2026-08-02` 在同一已消费私有集上的首次复验为
  24/24 执行、2 通过：来源章节不匹配 15 题、必需事实缺失 12 题、outcome 不匹配 8 题，
  安全 / 型号维度也未全过。原观测不改写，复验不恢复全新未见属性；`product/0.1.0` 仍未发布。
- 生成前边界只覆盖公开合成知识明确声明的安全事件和 CZ-R1 / CZ-R2 独占能力冲突，不宣称具备开放域安全分类能力。
- 不会自动发送、关闭工单、退款、换新或执行其他业务动作。
- 不提供登录、多租户、用户历史或生产客户系统集成。
- 使用单节点 SQLite 部署；不宣称生产级高可用或 SLA。
- 检索与生成仅覆盖小规模 CZ-R1/CZ-R2 语料；不宣称支持开放域。
- 16 题公开检索体检是开发集，不是未见 HOLDOUT；Top-5 / Top-10 数字只表示预标注的
  必需来源是否全部进入对应范围，不代表线上成功率、回答正确率或生成质量。
- 机械门通过不代表语言质量完美或真实世界召回完整；仍然需要人工审核。
