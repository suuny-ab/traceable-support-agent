# 项目事实

> 项目 ID：`AIJOB-20260711-01`
>
> 状态：`public_beta_live`
>
> 最后核实：`2026-07-31`

本文件只保存稳定产品事实。开发进度见 `docs/status.md`，路线见 `ROADMAP.md`，精选完成结果见 `docs/work/completed/`，历史过程由 Git 保存。

## 产品目的

这是一个可追溯客服决策支持 Workflow。真实 LLM 在宿主提供并绑定来源的证据上形成 QA 回复或工单处理候选，机械门负责检测来源、结构、安全和失败状态，客服主管作最终决定。

项目的现实用途是求职作品集：证明 RAG、受控 LLM Workflow、API、持久化、评测、安全边界、容器部署和可解释交付能力。

## 不可牺牲边界

- 可追溯：客户可见事实必须绑定来源。
- 失败关闭：无证据、安全、越界、结构或技术失败不得形成虚假成功。
- 人工最终决定：系统不自动发送、退款、换新或结单。
- 合成数据：不接触真实客户、个人信息、订单或生产系统。
- 费用可控：真实调用零自动重试，先预留预算，异常时停止。

## 当前能力

- 产品主页、设计说明、QA/工单体验和隐私页已通过 IP HTTPS 公开。
- 公共 API 支持异步 run、状态轮询、人工决定和健康检查。
- 公共控制面支持随机 run ID、精确 CORS、16 KiB 请求体限制、2 个运行中/4 个排队、浏览器软限、日/月硬预算和 30 天原文清理。
- 本地 QA 与工单主链已接入真实 LLM 两阶段生成、来源校验、SQLite 决定和失败转人工，并通过固定合成场景体验验收。
- 产品主链与公网 API 共用生成前确定性边界：声明的公开合成安全事件和 CZ-R1 请求 CZ-R2 独占集尘能力时，在 transport 构造前转人工；该能力只由本地合成回归证明，不构成新的未见集结论。
- 公网 Provider 已启用（`2026-07-29` 用户显式授权）；健康状态为 `available`，真实 QA 经绑定式溯源门（ADR-0007）生成候选并挂载证据原文，预算硬顶日 ¥20 / 月 ¥100。公开工作台为 live 优先：页面诚实
  检测实时状态；已验证回放为显式次要入口并保持可用（回放预览仍为 `replay_only` 语义）。
- 固定边界挑战 `GEN-DEV-IE-001` 是唯一例外：批准资料不足时，即使实时不可用，也会在
  Provider 调用前由确定性规则创建一次调用数为 0 的转人工运行；公开说明与该合同一致。
- 公网已验证回放为两个 QA 和一个工单预设；首页预览与批准来源 `KB-CZR1-014` 逐字一致。
- 公开源码仓库位于 <https://github.com/suuny-ab/traceable-support-agent>，并且是唯一权威开发仓库；公开仓库不等于产品成熟度晋级。
- GitHub 作品集展示已按产品结果、真实演示、架构、工程证据、限制和复现入口完成收口；
  README 与公网四页均区分实时运行和已验证回放，真实运行 GIF 不冒充预设回放。
- 公开设计页展示一次可重复的检索体检：16 个冻结合成问题覆盖 R1 / R2 各 8 题和当前
  27 个有效章节；BM25、BGE 的必需来源 Top-5 全覆盖均为 14/16，BM25+BGE+RRF 为
  16/16，三者 Top-10 均为 16/16、错误型号来源均为 0。该结果只证明这个公开开发集的
  来源覆盖，不是线上成功率、回答质量或未见集结论，全程未调用 Provider。
- 受保护 `main` 的 CI 全部成功后会按不可变镜像摘要自动部署生产；部署继续经过清单身份、SSH 预检、健康、回滚演练和正式回执门。

## 当前版本语义

- 稳定确定性基线：`demo-a/1.0`。
- 当前求职交付：公开 Beta 真实 Provider live 版，已验证回放作为独立降级入口。
- `product/0.1.0`：`not_released`。
- Stage 12 已执行一次：19/24 案例、9 通过，当时发现的 SAF-003 / MBD-003 边界缺陷已由 Issue #21 以公开合成回归修复，但未重跑未见集。
- Issue #22 已完成失败分类、宿主机械投影和 clause 级来源绑定并通过冻结候选复核；原候选生成质量完成门未满足，v15 真实 Provider 兼容性仍未知，已按求职收尾取舍停止开放域调优。
- 作品集视觉与公开回放体验先于 live 上线完成并通过独立生产体验验收；`2026-07-29`
  经独立授权启用真实 Provider，并以绑定式溯源门完成公网首跑。独立域名仍不纳入当前求职收尾。

## 公开接口

- `POST /api/v1/runs`
- `GET /api/v1/runs/{run_id}`
- `POST /api/v1/runs/{run_id}/decision`
- `GET /api/v1/health`

## 数据与外部依赖

- 六份虚构 CZ-R1/CZ-R2 合成知识资料。
- SQLite 仅保存公开体验运行和人工决定；原始输入最长保留 30 天。
- DeepSeek 凭据只存在服务器受限环境文件，不进入 Git、流水线或镜像。
- 当前阿里云部署为单机 Beta，不宣称生产级高可用或 SLA。

## 明确不包含

- 真实客户、真实客服账号、订单、设备或工单系统。
- 自动业务动作、开放式 Agent、多 Agent、自主工具循环或长期记忆。
- 多租户登录、生产级并发、高可用、完整监控或合规生产部署。
- 未经批准的 Stage 12 输入、费用或 Provider 调用。
